# Build dependencies for the docs site.
# Pin exact versions for reproducibility.
# Bump deliberately, in its own PR, with a build-and-deploy check.

mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocs-material-extensions==1.3.1

# Mermaid diagrams
mkdocs-mermaid2-plugin==1.2.1

# Versioned docs (multiple Metashape major-version branches)
mike==2.1.3

# Link checking (run by CI)
linkchecker==10.5.0

# Markdown linting (CI)
pymarkdownlnt==0.9.25
