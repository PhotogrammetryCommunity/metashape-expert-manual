# <Feature Name>

> **Status:** stub | draft | published
> **Last reviewed:** YYYY-MM-DD by `<reviewer-handle>`

## What it is

One short paragraph: the feature's purpose in the user's mental model,
not the developer's. Avoid algorithmic detail unless the algorithm IS
the user-visible thing. Do not over-explain — link to articles for
depth.

## Where it lives

| | Standard | Pro |
|---|---|---|
| **GUI** | <menu path or "n/a"> | <menu path or "(same)"> |
| **Python** | `Metashape.<Class>.<method>(<args>)` | (Pro only — Python is Pro-exclusive) |
| **CLI / batch** | <if applicable> | <if applicable> |
| **Available since** | <Standard version> | <Pro version> |

## Documented in the official manual

- *<Manual name>*, §<section>, p.<page> — short note on what's covered.
- *Python API Reference*, `Metashape.<Class>.<method>` — short note.

If the manual coverage is sparse, say so explicitly — that *is* the
reason this page exists.

## Related concepts

- [<feature>](<feature>.md) — relation in one phrase.
- See also: [glossary entry](../glossary.md#anchor) where applicable.

## Articles in this manual

List of expert-manual articles that recommend, warn against, or
otherwise discuss this feature, with one-line context per article. Do
not duplicate the article — link to it.

- [<Article title>](../../workflow/<stage>/<slug>.md) — context.

## Forum threads worth reading

A curated list of forum threads containing substantive discussion. Each
entry follows the citation format from `STYLE.md`.

| Date | Version | Author | Thread | One-line takeaway |
|------|---------|--------|--------|-------------------|
| YYYY-MM-DD | <Cite-as from version-timeline> | <Author> | [<thread title>](<permalink>) | <takeaway>. |

## Caveats and known interactions

- Version-specific behaviour (1.x vs 2.x).
- Documented interactions with related features.
- Common misuses and what they look like.

## How to extend this page

Add a row to *Forum threads worth reading* whenever a triage decision
flags an insight card as relating to this feature. Promote frequent
takeaways into a new article in `docs/workflow/...` or
`docs/topics/...`, then link the article from *Articles in this manual*
above.
